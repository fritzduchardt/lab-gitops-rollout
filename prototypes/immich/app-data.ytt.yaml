#@data/values-schema
#@overlay/match-child-defaults missing_ok=True
---
helm:
  #! immich helm chart is not consistently supporting namespace
  namespace: immich
application:
  namespace:
    create: true
