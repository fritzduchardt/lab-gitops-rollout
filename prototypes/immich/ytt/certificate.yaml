apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: immich-certificate
spec:
  secretName: immich-certificate
  duration: 2160h
  renewBefore: 360h
  subject:
    organizations:
      - friclu
  dnsNames:
    - immich.duchardt.net
  issuerRef:
    name: http01
    kind: ClusterIssuer
