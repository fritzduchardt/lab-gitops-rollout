controller:
  service:
    type: NodePort
  config: {
    # don't restrict body size
    proxy-body-size: 0
  }
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
  allowSnippetAnnotations: true
