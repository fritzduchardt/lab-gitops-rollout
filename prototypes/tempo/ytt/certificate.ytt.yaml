#@ load("@ytt:data", "data")
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tempo-certificate
spec:
  secretName: tempo-certificate
  duration: 2160h
  renewBefore: 360h
  subject:
    organizations:
      - friclu
  dnsNames:
    - tempo.duchardt.net
  issuerRef:
    name: http01
    kind: ClusterIssuer
