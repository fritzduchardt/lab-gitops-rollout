apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    name: immich-library
  name: immich-library
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: ""
