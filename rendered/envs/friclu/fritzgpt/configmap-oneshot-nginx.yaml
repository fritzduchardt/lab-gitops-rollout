apiVersion: v1
data:
  oneshot.conf: |
    server {
      listen 8080;
      server_name _;

      auth_basic "Authentication Required";
      auth_basic_user_file /etc/nginx/auth/.htpasswd;

      location /backend/ {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        rewrite ^/backend/(.*)$ /$1 break;
        proxy_pass http://oneshot:8000;
      }

      # SSE streaming endpoint - disable buffering to forward chunks immediately
      location /backend/stream {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
        # Disable buffering so SSE events are forwarded immediately
        proxy_buffering off;
        proxy_cache off;
        # Keep connection alive for SSE
        proxy_set_header Connection '';
        chunked_transfer_encoding on;
        rewrite ^/backend/(.*)$ /$1 break;
        proxy_pass http://oneshot:8000;
      }

      location /image/ {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_pass http://oneshot:8000;
      }

      location ~* ^/(manifest\.json|sw\.js)$ {
        auth_basic off;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_pass http://oneshot-ui:3000;
      }

      location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_pass http://oneshot-ui:3000;
      }
    }
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/component: nginx
    app.kubernetes.io/name: oneshot
  name: oneshot-nginx
  namespace: fritzgpt
