apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fabric-rendered-patterns
  namespace: n8n
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: n8n
  volumeMode: Filesystem
  volumeName: fabric-rendered-patterns
