apiVersion: v1
kind: PersistentVolume
metadata:
  name: immich-library
  namespace: immich
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 100Gi
  hostPath:
    path: /volumes/immich
  persistentVolumeReclaimPolicy: Retain
  storageClassName: immich
