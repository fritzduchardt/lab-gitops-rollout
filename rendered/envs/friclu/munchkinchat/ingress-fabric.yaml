apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/auth-realm: Authentication Required
    nginx.ingress.kubernetes.io/auth-secret: munchkinchat-basic-auth
    nginx.ingress.kubernetes.io/auth-type: basic
  labels:
    app: fabric
  name: fabric
  namespace: munchkinchat
spec:
  ingressClassName: nginx
  rules:
    - host: munchkinchat-friclu.duckdns.org
      http:
        paths:
          - backend:
              service:
                name: fabric-ui
                port:
                  number: 80
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - munchkinchat-friclu.duckdns.org
      secretName: fabric-certificate
