apiVersion: v1
kind: PersistentVolume
metadata:
  name: vault-data
  namespace: vault
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi
  hostPath:
    path: /volumes/vault
  persistentVolumeReclaimPolicy: Retain
