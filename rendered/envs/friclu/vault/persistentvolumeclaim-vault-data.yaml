apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    name: vault-data
  name: vault-data
  namespace: vault
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
