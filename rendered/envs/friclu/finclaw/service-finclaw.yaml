apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: finclaw
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: finclaw
    app.kubernetes.io/version: 1.0.0
    helm.sh/chart: finclaw-0.1.0
  name: finclaw
  namespace: finclaw
spec:
  ports:
    - name: http
      port: 8000
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/instance: finclaw
    app.kubernetes.io/name: finclaw
  type: ClusterIP
