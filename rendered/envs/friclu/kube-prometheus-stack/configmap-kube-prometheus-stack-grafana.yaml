apiVersion: v1
data:
  grafana.ini: |
    [analytics]
    check_for_updates = true
    [auth.generic_oauth]
    allow_sign_up = true
    api_url = https://fricloak.duckdns.org/realms/master/protocol/openid-connect/userinfo
    auth_url = https://fricloak.duckdns.org/realms/master/protocol/openid-connect/auth
    client_id = grafana
    client_secret = <path:kv/data/kube-prometheus-stack/grafana-client-secret#secret>
    email_attribute_path = email
    enabled = true
    login_attribute_path = username
    name = Keycloak-OAuth
    name_attribute_path = full_name
    role_attribute_path = contains(roles[*], 'admin') && 'Admin' || contains(roles[*], 'editor') && 'Editor' || 'Viewer'
    scopes = openid email profile offline_access roles
    token_url = https://fricloak.duckdns.org/realms/master/protocol/openid-connect/token
    [grafana_net]
    url = https://grafana.net
    [log]
    mode = console
    [paths]
    data = /var/lib/grafana/
    logs = /var/log/grafana
    plugins = /var/lib/grafana/plugins
    provisioning = /etc/grafana/provisioning
    [server]
    domain = frifana.duckdns.org
kind: ConfigMap
metadata:
  labels:
    app.kubernetes.io/instance: kube-prometheus-stack
    app.kubernetes.io/name: grafana
    app.kubernetes.io/version: 12.0.0
    helm.sh/chart: grafana-9.0.0
  name: kube-prometheus-stack-grafana
  namespace: kube-prometheus-stack
