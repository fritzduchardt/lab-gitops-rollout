apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sync-dir
  namespace: isabellabot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: sync-dir
