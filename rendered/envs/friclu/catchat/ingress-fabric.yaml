apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/auth-realm: Authentication Required
    nginx.ingress.kubernetes.io/auth-secret: catchat-basic-auth
    nginx.ingress.kubernetes.io/auth-type: basic
  labels:
    app: fabric
  name: fabric
  namespace: catchat
spec:
  ingressClassName: nginx
  rules:
    - host: catchat-friclu.duckdns.org
      http:
        paths:
          - backend:
              service:
                name: fabric-ui
                port:
                  number: 80
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - catchat-friclu.duckdns.org
      secretName: fabric-certificate
